% macros/web.tex - Web-specific macros for leanblueprint
% These are typically provided by the leanblueprint tool but we define fallbacks

% ============================================================
% Leanblueprint macros (fallbacks if not provided by tool)
% ============================================================

% Lean code reference
\providecommand{\lean}[1]{\texttt{#1}}

% Mark as formalized in Lean
\providecommand{\leanok}{}

% Mark as not yet formalized
\providecommand{\notready}{}

% Dependency declarations
\providecommand{\uses}[1]{}
\providecommand{\proves}[1]{}

% Project URL macros (typically overridden by leanblueprint)
\providecommand{\home}[1]{}
\providecommand{\github}[1]{}
\providecommand{\dochome}[1]{}
